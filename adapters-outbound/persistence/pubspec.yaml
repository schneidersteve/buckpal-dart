name: adapters_inbound_rest
description: A starting point for Dart libraries or applications.
version: 1.0.0
# repository: https://github.com/my_org/my_repo
publish_to: none

environment:
  sdk: '>=3.0.0-218.1.beta <4.0.0'

# Add regular dependencies here.
dependencies:
  domain:
    path: ../../domain
  application:
    path: ../../application

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
  http: ^0.13.5
  # mocktail: ^0.3.0
